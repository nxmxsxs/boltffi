---
import CodeComparison from './CodeComparison';

interface Props {
  rust: string;
  swift: string;
  kotlin: string;
  java?: string;
  typescript?: string;
  title?: string;
}

const { rust, swift, kotlin, java, typescript, title } = Astro.props;
---

<CodeComparison
  rust={rust}
  swift={swift}
  kotlin={kotlin}
  java={java}
  typescript={typescript}
  title={title}
  client:load
/>
